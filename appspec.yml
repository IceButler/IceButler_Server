version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/ice
permissions:
  - object: /home/ubuntu/ice/
    owner: ubuntu
    group: ubuntu
hooks:
  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 60
      runas: ubuntu
